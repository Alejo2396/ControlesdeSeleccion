<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tuapp.controldeseleccion">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ControlesDeSeleccion">
        <activity
            android:name=".ConexionBd"
            android:exported="false" />
        <!-- Activity nueva (opcional si quieres usarla) -->
        <activity
            android:name=".TeamsActivity"
            android:exported="false" /> <!-- Activity Lista -->
        <activity
            android:name=".Lista"
            android:exported="false" /> <!-- Activity Datos (pantalla principal) -->
        <activity
            android:name=".Datos"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Activity donde calculas totales (si existe también) -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
    </application>

</manifest>